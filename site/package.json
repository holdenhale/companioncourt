{
  "name": "companioncourt-site",
  "version": "0.1.0",
  "description": "CompanionCourt static site build (markdown -> HTML)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node dev.mjs",
    "build": "node build.mjs",
    "check": "node build.mjs --check && node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "marked": "18.0.5"
  }
}
