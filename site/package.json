{
  "name": "companioncourt-site",
  "version": "0.1.0",
  "description": "CompanionCourt static site build (markdown -> HTML)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs --check"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "marked": "18.0.5"
  }
}
