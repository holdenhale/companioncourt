# dependencies
node_modules/

# build output (regenerated by `npm install` via the prepare script).
# Anchored to the repo root so the tracked site build (site/dist/) is untouched.
/dist/
*.tsbuildinfo

# runtime artifacts from the quickstart (bring-your-own-endpoint runs)
/runs/
/board/
/anchor-pack.json
